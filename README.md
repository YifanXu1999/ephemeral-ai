# Ephemeral-AI

This repository composes the Ephemeral AI workspace from two submodules:

- `ephemeral-agent`
- `ephemeral-os`

After cloning, initialize the submodules with:

```bash
git submodule update --init --recursive
```
